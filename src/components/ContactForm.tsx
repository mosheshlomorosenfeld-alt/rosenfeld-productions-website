'use client';
import {FormEvent,useState} from 'react';
export function ContactForm(){const [status,setStatus]=useState('');function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const subject=String(f.get('subject')||'Rosenfeld Productions Inquiry');const body=`Name: ${f.get('name')}\nEmail: ${f.get('email')}\n\nMessage:\n${f.get('message')}`;window.location.href=`mailto:mosheybc11@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;setStatus('Your email app is opening with your message filled in.');}return <section id="contact" className="px-6 py-28 md:px-12"><div className="mx-auto grid max-w-4xl gap-10 md:grid-cols-2"><div><p className="text-xs tracking-[.35em] text-acid">CONTACT ROSENFELD PRODUCTIONS</p><h2 className="mt-4 text-5xl font-black md:text-7xl">START A CONVERSATION.</h2><p className="mt-6 leading-7 text-mist">For music, production, recording, songwriting, performances and collaborations.</p></div><form onSubmit={submit} className="grid gap-4"><input required name="subject" placeholder="SUBJECT" className="border border-white/15 bg-white/[.03] p-4 outline-none"/><input required name="name" placeholder="NAME" className="border border-white/15 bg-white/[.03] p-4 outline-none"/><input required type="email" name="email" placeholder="EMAIL" className="border border-white/15 bg-white/[.03] p-4 outline-none"/><textarea required name="message" rows={6} placeholder="HOW CAN I HELP?" className="border border-white/15 bg-white/[.03] p-4 outline-none"/><button className="bg-acid px-6 py-4 text-xs font-black tracking-widest text-black">EMAIL ROSENFELD PRODUCTIONS</button>{status&&<p className="text-sm text-acid">{status}</p>}</form></div></section>}