'use client';import {useState} from 'react';import {tracks} from '../data/catalog';import {Role} from '../types';
const filters:(Role|'All')[]=['All','Production','Songwriting','Vocals'];export function Discography(){const [filter,setFilter]=useState<Role|'All'>('All');const list=filter==='All'?tracks:tracks.filter(t=>t.roles.includes(filter));return <section id="catalog" className="mx-auto max-w-7xl px-6 py-28 md:px-12"><p className="text-xs tracking-[.35em] text-acid">SELECTED CATALOG</p><h2 className="mt-4 text-5xl font-black md:text-8xl">THE WORK.</h2><div className="mt-8 flex flex-wrap gap-2">{filters.map(f=><button key={f} onClick={()=>setFilter(f)} className={`border px-4 py-2 text-xs ${filter===f?'border-acid bg-acid text-black':'border-white/15 text-mist'}`}>{f}</button>)}</div><div className="mt-10 grid gap-4 md:grid-cols-3">{list.map(t=><article key={t.id} className="group overflow-hidden border border-white/10 bg-white/[.03]"><img src={t.artwork} alt="" className="h-72 w-full object-cover transition duration-700 group-hover:scale-105"/><div className="p-5"><div className="flex justify-between text-xs text-mist"><span>{t.year}</span><span>{t.duration}</span></div><h3 className="mt-3 text-3xl font-black">{t.title}</h3><p className="mt-2 text-sm text-mist">{t.mood}</p><div className="mt-5 flex gap-4 text-xs font-bold"><a href={t.links.spotify} target="_blank">SPOTIFY</a><a href={t.links.apple} target="_blank">APPLE</a><a href={t.links.amazon} target="_blank">AMAZON</a></div></div></article>)}</div></section>}